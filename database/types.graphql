type User {
  id: ID! @unique
  email: String
  emailVerified: Boolean @default(value: false)
  auth0Id: String @unique
  identity: String
  role: Role @default(value: USER)
  name: String
  avatar: String
}
